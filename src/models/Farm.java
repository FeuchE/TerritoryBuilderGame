package models;

// Creating Farm class within Building class
public class Farm extends Building {

    // Function to assign the name of a farm
    public Farm(String name) {
        super(name);
    }
}