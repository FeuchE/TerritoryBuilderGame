package models;

// Creating Forge class within Building class
public class Forge extends Building {

    // Function to assign the name of a forge
    public Forge(String name) {
        super(name);
    }
}
